# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native builds
ios/
android/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/

# Vercel
.vercel/

# TypeScript
*.tsbuildinfo
.vercel
.env*.local

# Secrets
AuthKey_*.p8
credentials.json
tokim
eple.txt
build-info.json
DEPLOY.md

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Source asset archives — kept out of the tree per Apple-compliance steering.
# The optimized 256x256 PNGs derived from these archives DO ship in
# `assets/pixel-icons/` (see scripts/optimize-pixel-icons.ps1 if we
# rebuild the pipeline), but the multi-megabyte raw zips never enter
# version control.
telegram_ios_settings_icon_pack*.zip
pixel_characters_pack*.zip

# Cloudflare Worker scaffold — the Worker has its own package.json and
# package-lock.json (committed). Local-only state must stay out of git.
workers/api/node_modules/
workers/api/.dev.vars
workers/api/.wrangler/
workers/api/dist/
