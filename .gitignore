# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native builds
ios/
android/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/

# Vercel
.vercel/

# TypeScript
*.tsbuildinfo
.vercel
.env*.local

# Secrets
AuthKey_*.p8
credentials.json
tokim
eple.txt
build-info.json
DEPLOY.md

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Source asset archives — kept out of the tree per Apple-compliance steering.
# The optimized 256x256 PNGs derived from these archives DO ship in
# `assets/pixel-icons/` (see scripts/optimize-pixel-icons.ps1 if we
# rebuild the pipeline), but the multi-megabyte raw zips never enter
# version control.
telegram_ios_settings_icon_pack*.zip
pixel_characters_pack*.zip

# Raw logo/icon iteration exports and Apple's SF Symbols installer. Same rule
# as above: only the optimized, shipped assets under `assets/` belong in git.
logoappekpan/
sanemageekon.zip
SF-Symbols-*.dmg

# Local-only Worker scratch: a dump of the LIVE deployed bundle plus the
# prepend/extract helpers used to hot-patch it, and the wrangler state that
# comes with them. The real Worker source is `workers/api/src/` (committed).
/worker/
worker_dump.txt
worker_live_check.txt

# Scratch commit-message buffer.
.git_commit_msg.txt

# Cloudflare Worker scaffold — the Worker has its own package.json and
# package-lock.json (committed). Local-only state must stay out of git.
workers/api/node_modules/
workers/api/.dev.vars
workers/api/.wrangler/
workers/api/dist/
